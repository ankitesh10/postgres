# Overview

## Psql Commands

```text
\l -> list database
\? -> help
\! -> allow to run bash command
\! pwd -> working directory
\d -> show tables, sequences, constraints etc
```

## Create And Connect

```sql
CREATE DATABASE recipeguru;
```

Connect to database:

```text
\c recipeguru
```

## Create Table

```sql
CREATE TABLE ingredients (
id INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
title VARCHAR( 255 ) UNIQUE NOT NULL
);
```

`GENERATED ALWAYS AS IDENTITY` is serial and auto-incrementing. Previously, the `serial` command was used in Postgres.

## Insert

```sql
INSERT INTO ingredients (title) VALUES ('bell pepper');
```

## Read

```sql
SELECT * FROM ingredients;
```

## Drop

```sql
DROP TABLE ingredients;
DROP DATABASE recipeguru;
```

## Alter Table

```sql
ALTER TABLE ingredients ADD COLUMN image VARCHAR ( 255 );
```

Show table schema:

```text
\d ingredients
```

Drop column:

```sql
ALTER TABLE ingredients DROP COLUMN image;
```

Add type:

```sql
ALTER TABLE ingredients
ADD COLUMN type VARCHAR (50) NOT NULL;
```

Multiple commands:

```sql
ALTER TABLE ingredients
ADD COLUMN image VARCHAR (255),
ADD COLUMN type VARCHAR (50) NOT NULL;

ALTER TABLE ingredients DROP COLUMN image;
```

Add default value:

```sql
ALTER TABLE ingredients
ADD COLUMN image VARCHAR ( 255 ),
ADD COLUMN type VARCHAR ( 50 ) NOT NULL DEFAULT 'vegetable';
```
